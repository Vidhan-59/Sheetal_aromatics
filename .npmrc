# Several shadcn/ui dependencies (vaul, react-day-picker) still declare React 18
# peer ranges while this project runs React 19. Installs must therefore be
# tolerant of those peer ranges — without this, `npm ci` fails on Vercel.
legacy-peer-deps=true
